# Known-failing e2e tests — the suite's honest baseline.
#
# EMPTY. As of 2026-07-15 every test in this suite passes: 221 passed, 0 known-failing,
# measured by scripts/e2e.sh against a container built from source.
#
# It was 27 when the harness was first built (196 passed / 27 failed). All 27 were
# triaged one at a time — run it, read the real failure, read the source, decide REAL BUG
# or STALE TEST, fix, re-run green. The tally: 26 stale tests, 1 real bug.
#
# The stale ones were not junk — they were true when written and never caught up. Two
# structural changes account for nearly all of them:
#   1. #security grew from one DataTable to many, so unscoped `table.dt tbody tr`
#      selectors swept 754 rows instead of 3 and never reached their assertions. Every
#      behavioural assertion passed FIRST TRY, unmodified, once scoped.
#   2. The topbar consolidated behind the ⋯ overflow (⋯ -> gear -> control), whose panel
#      is display:none when closed. Density, Wall, Watches, theme, saved views all moved.
# The real bug: OverviewTab never rendered a SynthBand — the landing tab was the only one
# that showed raw counters instead of an answer.
#
# KEEP IT EMPTY. e2e.sh fails the run when the failing set changes, so a new line here
# means someone chose to accept a failure. Prefer fixing. If you must add one, add the
# reason with it — a bare line is indistinguishable from a bug nobody looked at.
#
# One test per line, exactly as Playwright reports it: <file> :: <title>
