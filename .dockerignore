# Never ship secrets or local state into the image
.env
.mcp.json
dns_clients_cache.json
ruvector.db

# Virtualenvs / caches
.venv
.venv.broken
__pycache__
*.pyc
*.pyo

# Dev cruft
.git
.gitignore
.DS_Store
90%
test_regression.py
Dockerfile
.dockerignore
run.sh
README.docker.md

# Phase 1 build-time only — the runtime loads the precompiled app.bundle.js, so the
# in-browser Babel and the JSX source never need to ship in the image.
babel.min.js
src
scripts/build_ui.js
scripts/_carve_once.py
